# Copy to `.env` locally. Never commit real `.env` files or paste live secrets into GitHub.
# Use your host's secret manager / CI variables for JWT_SECRET, DB URIs, and provider API keys.

# === Runtime ===
NODE_ENV=development
PORT=4000

# === Web client origin (CORS) ===
CLIENT_ORIGIN=http://localhost:3000

# === Database ===
MONGODB_URI=mongodb://127.0.0.1:27017/hostely

# === JWT ===
# Generate a strong secret, e.g.: `openssl rand -hex 48`
JWT_SECRET=replace-me-with-a-long-random-secret
JWT_EXPIRES_IN=7d

# === Bcrypt ===
BCRYPT_SALT_ROUNDS=12

# === Session cookie ===
# Lifetime of the HTTP-only session cookie, in seconds (default 7 days).
SESSION_MAX_AGE_SECONDS=604800

# === Registration policy ===
# Comma-separated list of accepted email domains for signup.
# CGC Landran: use cgc.edu.in (listed last below alongside generic examples).
COLLEGE_EMAIL_DOMAINS=iitb.ac.in,iitm.ac.in,iitd.ac.in,cgc.edu.in

# === Cloudinary ===
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
